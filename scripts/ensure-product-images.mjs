/**
 * Product display images come from src/store/productImageUrls.ts (unique external URLs).
 * Local files in public/images/products/ are optional fallbacks only.
 */
console.log("Product images: using unique external URLs from productImageUrls.ts (160 URLs).");
