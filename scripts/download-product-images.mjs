/**
 * Mock download-product-images.mjs.
 * All product images are now copied locally in ensure-product-images.mjs to avoid network scrapers.
 */
console.log("Product images are handled locally by ensure-product-images.mjs. Skipping downloads.");
